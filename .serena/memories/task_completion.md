# Task completion checklist

- Build/run in Xcode to confirm SwiftUI app launches.
- Run unit/UI tests from Xcode if applicable (schemes not shared in repo).
- If localization changes are made, verify in Xcode with different simulator/device language settings.
