# Suggested commands

- Open the app in Xcode: `open Routyra/Routyra.xcodeproj`.
- Run/build/tests: use Xcode (Product > Run / Test). No shared Xcode scheme or CLI test command found in repo; verify scheme name in Xcode if you want `xcodebuild` commands.
